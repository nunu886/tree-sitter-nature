var a_123 = f
string? a = f
string?|int|null|i8 a = f
const a = f
type MyType = string?|int|null|i8
const b = 123
const b = 1.2
const str = "Hello"
const c = 'a'
int? a = null
const a = true
const b = false
const arr = [1, 2, 3]
const arr2 = ["a", "b"]

/* Comment
*
*
/
// comment a

//type decl
type S = struct {
    string field1
    int field2
}
(int, string) a = (1, "a")

//function call
const call = f()
const call2 = f(1, "a")
const call3 = f(1, "a", (1,2))
const call4 = f((1, f()))

// if statement
if a {
    var a = 1
} else if 1 {
    var a = 2
} else {
    const a = f()
}

// [int] a = [1,2,3]
[int] a = [1,2,3]
[int] b = [1,2,3]

// for statement
for 1 {

}
for var a = 1; 1; 1 {
}
for k in map {
}

//function decl
fn f(int a) {
    const a = b()
}

fn f(int a, float b, string c, bool d) {
    const a = b(a,b,c,d)
}

fn f(T a) {
    const a = b(a)
}

fn sum<T>(T a, T b):T {
   const a = b(a,b)
}
fn sum<T,V,W>(T a, V b, W c):(T,V,W) {
   const a = b(a,b,c)
}

//gen fun call
//const a = b<int>(1,2)

// tuple destructuring declaration
var (t1, t2, t3) = (1, true, "hello")

// var function declaration
var f = fn() {
    const a = b()
}

//call statement
f(1,2,3)

//f<int>(1,2,3)
